import React, {Component, PropTypes,} from 'react';

import Header from './Header';

class App extends Component {
	render() {
		return (
			<Header />
		);
	}
}

App.propTypes = {};
App.defaultProps = {};

export default (App);
