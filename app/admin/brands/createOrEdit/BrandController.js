import React, {Component, PropTypes,} from 'react';

class BrandController extends Component {
	render() {
		return (
			<div></div>
		);
	}
}

BrandController.propTypes = {};
BrandController.defaultProps = {};

export default (BrandController);
